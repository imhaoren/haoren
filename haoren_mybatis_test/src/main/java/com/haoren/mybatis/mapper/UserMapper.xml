<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="com.haoren.mybatis.mapper.UserMapper">

    <select id="selectOne" parameterType="java.lang.Integer" resultType="com.haoren.mybatis.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="selectAll" resultType="com.haoren.mybatis.entity.User">
        select * from user
    </select>
</mapper>